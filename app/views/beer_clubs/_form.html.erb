<%= form_with(model: beer_club) do |form| %>
  <% if beer_club.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
        <% beer_club.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label :name, class: "form-label" %>
  <%= form.text_field :name, class: "form-control text-bg-dark  mb-3" %>

  <%= form.label :founded, class: "form-label" %>
  <%= form.number_field :founded, class: "form-control text-bg-dark  mb-3" %>

  <%= form.label :city, class: "form-label" %>
  <%= form.text_field :city, class: "form-control text-bg-dark  mb-3" %>

  <%= form.submit class: "btn bg-dark btn-primary" %>
<% end %>
